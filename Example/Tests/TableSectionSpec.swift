import Foundation
import UIKit
import Quick
import Nimble

import TableViewModel

class TableSectionSpec: QuickSpec {
    override func spec() {
        describe("TableSection") {
            var tableSection: TableSection?

            beforeEach {
            }

            context("when added to a table view model") {

            }
        }
    }
}